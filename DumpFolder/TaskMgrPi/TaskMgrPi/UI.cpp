#include "UI.h"
#include <stdio.h>
#include <string>

int UI::main(int argc, const char * argv[])
{
	std::string input;
	input = scanf("%s");
	//TODO: create main entrypoint of program. Takes input, creates task, makes output.
	return 0;
}
