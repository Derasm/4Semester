#define WIDTH 10 // no need for semicolon as it is a preprocessor command.
#define HEIGHT 10
class CtrSuper
{
private:
    //grid for navigation purposes.
    int grid[WIDTH][HEIGHT];
public:
    CtrSuper(/* args */);
    ~CtrSuper();
};
